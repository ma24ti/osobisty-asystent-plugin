# ============================================================================
# {{DISPLAY_NAME}} — szablon .env
# ============================================================================
# Skopiuj do `.env` w katalogu swojego workspace'a (jeśli .env istnieje —
# dopisz brakujące klucze). Wypełnij TYLKO wartości skilli, których używasz.
# Sekretów NIGDY nie commituj (.env jest w .gitignore).
# Skąd wziąć każdy klucz — patrz plugins/{{PLUGIN}}/requirements.json.
# ============================================================================
